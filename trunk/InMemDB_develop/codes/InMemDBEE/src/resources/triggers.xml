<?xml version="1.0" encoding="UTF-8"?>
<triggers xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:noNamespaceSchemaLocation="triggers.xsd">
	<!-- 内存数据库中的触发器，有些是内置的，比如集群触发、持久备份触发等，不在这里定义，由相关的模块自动生成 -->

	<!-- 这里定义的是业务上需要处理的触发器，如行数限制，数据处理监听，数据时效性限制等 -->

	<!-- ‘table’ 是触发器所在的表 -->
	<!-- ‘type’有4种类型，DELETE, UPDATE, INSERT, SELECT -->
	<!-- ‘before’为true时在触发语句执行前执行触发器方法，false为之后执行 -->
	<!-- ‘class’是触发器代表类，必须是com.creaway.inmemdb.trigger.InMemDBTrigger的子类 -->
	<!-- 'command'是外部注册命令，通过注册该命令的触发器，可以指定执行相关监听 -->
	<!-- ‘description’ 是对该触发器执行功能的解释和描述 -->

	<!-- <trigger table="TTTT" name="Persistent_UPD" type="UPDATE" before="false" 
		eachrow="true" class="com.creaway.inmemdb.persistent.BackupTrigger" command="1000" 
		description="backup trigger" /> -->
    <!-- -->
	<group>
		<trigger table="TTTT" name="PER_INS" type="INSERT"
			before="false" eachrow="true" class="com.creaway.inmemdb.trigger.PushTriggerTest"
			command="1001" description="test trigger" />
		<trigger table="TTTT" name="PER__UPD" type="UPDATE"
			before="false" eachrow="true" class="com.creaway.inmemdb.trigger.PushTriggerTest"
			command="1001" description="test trigger" />
		<trigger table="TTTT" name="PER__DEL" type="DELETE"
			before="false" eachrow="true" class="com.creaway.inmemdb.trigger.PushTriggerTest"
			command="1001" description="test trigger" />
	</group> 
	<group>
		
		<trigger table="TTTT" name="PER__INS2" type="INSERT"
			before="false" eachrow="true" class="com.creaway.inmemdb.trigger.PushTriggerTest"
			command="7010" description="test trigger" />
	</group>


</triggers>